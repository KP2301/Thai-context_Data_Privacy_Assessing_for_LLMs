pip install -q groq

pip install python-dotenv 

pip install jsonlines 

pip install rapidfuzz

pip install pandas

pip install transformers

pip install together

pip install pythainlp

pip install python-crfsuite

Analyze method: 
1. run gather_file.py for each language level folder
2. run gather_3_model.py 

DEA Language: 
1. run plot_graph_by_language.py

DEA Length: 
1. run add_length_count.py
2. run gather_file.py for each language level folder 
3. run gather_3_model.py 
4. run plot_graph_by_length_file_bar.py

DEA Token: 
1. run add_token_count.py
2. run gather_file.py for each language level folder 
3. run gather_3_model.py 
4. run plot_graph_by_token_file_bar.py

JA Language:
1. run jailbreak_rate.py
2. run ja_query_rate.py